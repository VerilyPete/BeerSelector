appId: org.verily.FSbeerselector
name: Offline Mode - Network State Detection
tags:
  - offline
  - network
  - e2e

---

# Test: Offline Mode with Network State Indicator
#
# This test verifies:
# 1. OfflineIndicator appears when device goes offline
# 2. OfflineIndicator disappears when device comes back online
# 3. Cached data is accessible when offline
# 4. Network-dependent operations show appropriate messages

# Launch the app
- launchApp

# Wait for app to load
- waitForAnimationToEnd

# Verify app loads normally when online
- assertVisible: "All Beer"

# Go to All Beers tab (if not already there)
- tapOn:
    text: "All Beers"
    retryTapIfNoChange: false

# Verify we can see beer list when online
- waitForAnimationToEnd
# `matching: contains` was here and is not a Maestro property. Maestro
# validates every flow in the directory before running any of them, so this one
# line aborted the entire suite with zero tests executed. Verified against the
# pinned CLI by reproducing it — see "How Maestro validates this suite", point
# 1, in README.md.
#
# Earlier revisions of this comment also claimed the abort happened "on both
# platforms" and "about twelve seconds in". Neither was ever observed: the
# Android jobs are known-red for an unrelated reason and never got this far,
# and no timing was recorded. The platform claim is a plausible inference —
# validation precedes any device work, so it should behave the same — but it is
# an inference, and it was being stated as an observation next to the admission
# that CI never reached this point.
#
# The line was also redundant: `text: ".*"` already matches anything, so
# removing it changes nothing this step asserts.
- scrollUntilVisible:
    element:
      text: ".*"
    direction: DOWN

# Enable airplane mode to simulate offline
- runFlow:
    when:
      platform: iOS
    commands:
      - openLink: "prefs:root=AIRPLANE_MODE"
      - tapOn: "Airplane Mode"
      - tapOn:
          point: "50%,10%"  # Tap status bar to go back
      - openLink: "beerselector://"

- runFlow:
    when:
      platform: Android
    commands:
      # Android: Enable airplane mode
      - setAirplaneMode: enabled
      - waitForAnimationToEnd

# Verify offline indicator appears
- assertVisible:
    text: "No Internet Connection"

# Verify we can still view cached beer data when offline
- assertVisible: "All Beer"

# Try to access different tabs (cached data should work)
- tapOn: "Beerfinder"
- waitForAnimationToEnd

# Offline indicator should still be visible
- assertVisible: "No Internet Connection"

# Go back to All Beer
- tapOn: "All Beers"
- waitForAnimationToEnd

# Disable airplane mode to go back online
- runFlow:
    when:
      platform: iOS
    commands:
      - openLink: "prefs:root=AIRPLANE_MODE"
      - tapOn: "Airplane Mode"
      - tapOn:
          point: "50%,10%"  # Tap status bar to go back
      - openLink: "beerselector://"

- runFlow:
    when:
      platform: Android
    commands:
      # Android: Disable airplane mode
      - setAirplaneMode: disabled
      - waitForAnimationToEnd

# Wait for network to reconnect
# Was `waitForAnimationToEnd: 5000`. The scalar form is not valid — the
# duration goes in a `timeout` key — and it produced the opaque "List is
# empty" parse error that was sitting behind the `matching` one above.
- waitForAnimationToEnd:
    timeout: 5000

# Verify offline indicator disappears (or becomes hidden)
# Note: May need to check that it's NOT visible
- evalScript: |
    try {
      output.visible = !maestro.findElement({ text: "No Internet Connection" });
    } catch (e) {
      output.visible = true; // Element not found = offline indicator hidden
    }

# Verify we can still navigate normally
- tapOn: "Beerfinder"
- waitForAnimationToEnd
- assertVisible: "Beerfinder"

# Test complete
