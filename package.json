{
  "name": "beerselector-contract-compatibility",
  "version": "1.0.0",
  "private": true,
  "description": "Legacy consumer contracts imported by VerilyPete/ufobeer; iOS builds independently in native-ios.",
  "scripts": {
    "test": "vitest run",
    "test:ci": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "vitest": "^4.1.11"
  }
}
