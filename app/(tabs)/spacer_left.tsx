import React from 'react';
import { View } from 'react-native';

// Empty screen component for spacer tab
export default function SpacerLeftScreen() {
  // Return an empty view - this screen should never be visible to the user
  return <View style={{ flex: 1 }} />;
} 